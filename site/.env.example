# Public encyclopedia — environment.
# Engine REST API base (read-only, public tier clamp applied server-side).
KGE_API_URL=http://localhost:8088
# Absolute site origin, used for sitemap.xml / robots.txt / canonical URLs.
SITE_URL=http://localhost:3099
